# ==================================
# PRODUCTION ENVIRONMENT
# ==================================

# -----------------------------------
# Application Environment
# -----------------------------------
NODE_ENV="production"

# Base URLs (use your production domains)
BASE_URL_APP="yourdomain.com"
BASE_URL_AUTH="yourdomain.com"

# -----------------------------------
# Ports Configuration
# -----------------------------------
# GraphQL API Port (internal, proxied by Caddy)
GRAPHQL_PORT=13000
PORT=13000

# Frontend Port (internal, proxied by Caddy)
FRONTEND_PORT=8080

# -----------------------------------
# SSL Configuration
# -----------------------------------
# In production, Caddy handles SSL
# Set to 'false' to use https://
NOT_USE_SSL=false

# -----------------------------------
# Database Configuration
# -----------------------------------
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="CHANGE_THIS_SECURE_PASSWORD"
POSTGRES_DB="divinoalimento"
POSTGRES_HOST="db.prod"
POSTGRES_PORT=5432
DB_HOST="db.prod"

# -----------------------------------
# OAuth 2.0 Configuration
# -----------------------------------
# Configure your OAuth provider for production
clientID='your_oauth_client_id'
issuerBaseURL='https://your-oauth-provider.com'

# -----------------------------------
# Frontend Configuration
# -----------------------------------
# GraphQL Endpoint for Frontend (use your production domain)
# This should match your Caddy reverse proxy configuration
VITE_GRAPHQL_ENDPOINT=https://yourdomain.com/graphql
