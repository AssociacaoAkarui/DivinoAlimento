version: "3.8"

services:
  app_tests.dev:
    image: node:22
    working_dir: /app
    command: sleep 4h
    volumes:
      - ./app:/app
      - /app/node_modules
    environment:
      - NODE_ENV=test
